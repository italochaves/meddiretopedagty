
// Service neutralized to prevent "API Key missing" errors on startup.
// The AI functionality has been disabled as per user request.

export const parsePrescriptionsFromTxt = async (fileContent: string) => {
    console.warn("Funcionalidade de IA desativada.");
    return [];
};
